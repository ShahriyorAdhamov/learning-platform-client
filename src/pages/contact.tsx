import { withLayout } from 'src/layouts/layout';
import { ContactPageComponent } from '../page-components';

const ContactPage = () => {
	return <ContactPageComponent/>;
};

export default withLayout(ContactPage);